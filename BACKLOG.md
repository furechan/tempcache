# Backlog

Items decided or considered but not scheduled. Add new items at the end.

## CI

- Migrate GitHub workflow from pip to uv (python-package.yml)
- Remove ruff from GitHub workflow (python-package.yml) — lint locally only
